/** Tailwind is included in the project for extension; the prototype's role-specific CSS lives in src/styles.css. */
export default { content: ['./index.html','./src/**/*.{js,ts,jsx,tsx}'], theme:{extend:{}}, plugins:[] }
